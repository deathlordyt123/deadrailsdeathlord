-- FULL Dead Rails TP Hub Enhanced GUI (Updated)
-- All updates: GUI shape, outline, teleport updates, fullbright, horse button, styled credits, back buttons, ESP/KillAura toggles, Horse Class button

-- Services
local Players = game:GetService("Players")
local Lighting = game:GetService("Lighting")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

-- GUI Setup
local ScreenGui = Instance.new("ScreenGui", PlayerGui)
ScreenGui.Name = "DeadRailsHub"
ScreenGui.ResetOnSpawn = false

-- Main Frame (Rectangle Shape with Cool Outline)
local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Size = UDim2.new(0, 500, 0, 400)  -- Taller frame for a more rectangular shape
MainFrame.Position = UDim2.new(0.5, -250, 0.5, -200)
MainFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 50)
MainFrame.BorderColor3 = Color3.fromRGB(200, 200, 255)
MainFrame.BorderSizePixel = 3
MainFrame.Active = true
MainFrame.Draggable = true

-- Header
local Header = Instance.new("Frame", MainFrame)
Header.Size = UDim2.new(1, 0, 0, 40)
Header.BackgroundColor3 = Color3.fromRGB(45, 45, 75)

local Title = Instance.new("TextLabel", Header)
Title.Text = "Dead Rails TP Hub"
Title.Size = UDim2.new(1, -80, 1, 0)
Title.Position = UDim2.new(0, 10, 0, 0)
Title.BackgroundTransparency = 1
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 22
Title.TextXAlignment = Enum.TextXAlignment.Left

local MinimizeButton = Instance.new("TextButton", Header)
MinimizeButton.Text = "_"
MinimizeButton.Size = UDim2.new(0, 40, 1, 0)
MinimizeButton.Position = UDim2.new(1, -80, 0, 0)
MinimizeButton.BackgroundColor3 = Color3.fromRGB(80, 80, 120)
MinimizeButton.TextColor3 = Color3.new(1, 1, 1)
MinimizeButton.Font = Enum.Font.GothamBold
MinimizeButton.TextSize = 20

local CloseButton = Instance.new("TextButton", Header)
CloseButton.Text = "X"
CloseButton.Size = UDim2.new(0, 40, 1, 0)
CloseButton.Position = UDim2.new(1, -40, 0, 0)
CloseButton.BackgroundColor3 = Color3.fromRGB(150, 50, 50)
CloseButton.TextColor3 = Color3.new(1, 1, 1)
CloseButton.Font = Enum.Font.GothamBold
CloseButton.TextSize = 20

-- Hub Button
local HubButton = Instance.new("TextButton", ScreenGui)
HubButton.Text = "Hub"
HubButton.Size = UDim2.new(0, 100, 0, 40)
HubButton.Position = UDim2.new(0.5, -50, 0.5, -20)
HubButton.BackgroundColor3 = Color3.fromRGB(90, 90, 120)
HubButton.TextColor3 = Color3.new(1, 1, 1)
HubButton.Font = Enum.Font.GothamBold
HubButton.TextSize = 18
HubButton.Visible = false
HubButton.Active = true
HubButton.Draggable = true

-- Tab Container
local TabFolder = Instance.new("Frame", MainFrame)
TabFolder.Size = UDim2.new(1, 0, 1, -40)
TabFolder.Position = UDim2.new(0, 0, 0, 40)
TabFolder.BackgroundTransparency = 1

local Tabs = {"Teleport", "Exploits", "Credits"}
local TabFrames = {}

for i, tabName in ipairs(Tabs) do
    local tabBtn = Instance.new("TextButton", TabFolder)
    tabBtn.Text = tabName
    tabBtn.Size = UDim2.new(0, 120, 0, 30)
    tabBtn.Position = UDim2.new(0, (i - 1) * 130 + 10, 0, 10)
    tabBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 90)
    tabBtn.TextColor3 = Color3.new(1, 1, 1)
    tabBtn.Font = Enum.Font.GothamBold
    tabBtn.TextSize = 14

    local contentFrame = Instance.new("Frame", TabFolder)
    contentFrame.Size = UDim2.new(1, -20, 1, -50)
    contentFrame.Position = UDim2.new(0, 10, 0, 50)
    contentFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
    contentFrame.Visible = i == 1
    TabFrames[tabName] = contentFrame

    tabBtn.MouseButton1Click:Connect(function()
        for _, frame in pairs(TabFrames) do
            frame.Visible = false
        end
        contentFrame.Visible = true
    end)

    -- Back Button in Each Tab
    local backBtn = Instance.new("TextButton", contentFrame)
    backBtn.Text = "< Back"
    backBtn.Size = UDim2.new(0, 80, 0, 25)
    backBtn.Position = UDim2.new(0, 10, 1, -35)
    backBtn.BackgroundColor3 = Color3.fromRGB(90, 90, 120)
    backBtn.TextColor3 = Color3.new(1, 1, 1)
    backBtn.Font = Enum.Font.GothamBold
    backBtn.TextSize = 14
    backBtn.MouseButton1Click:Connect(function()
        contentFrame.Visible = false
    end)
end

-- Teleport Buttons
local teleportButtons = {
    {name = "TP TO STERLING", url = "https://raw.githubusercontent.com/ringtaa/sterlingnotifcation.github.io/main/Sterling.lua"},
    {name = "TP TO CASTLE", url = "https://raw.githubusercontent.com/ringtaa/castletpfast.github.io/main/FASTCASTLE.lua"},
    {name = "TP TO FORT", url = "https://raw.githubusercontent.com/ringtaa/Tpfort.github.io/refs/heads/main/Tpfort.lua"},
    {name = "TP TO END", coordinates = Vector3.new(-424, 30, -49041)},
    {name = "TP TO TESLA LAB", url = "https://raw.githubusercontent.com/ringtaa/tptotesla.github.io/main/Tptotesla.lua"},
    {name = "TP TO TOWN", url = "https://raw.githubusercontent.com/ringtaa/Tptobank.github.io/refs/heads/main/Banktp.lua"},
    {name = "TP TO TRAIN", url = "https://raw.githubusercontent.com/ringtaa/train.github.io/refs/heads/main/train.lua"}
}

for i, info in ipairs(teleportButtons) do
    local btn = Instance.new("TextButton", TabFrames["Teleport"])
    btn.Text = info.name
    btn.Size = UDim2.new(1, -20, 0, 30)
    btn.Position = UDim2.new(0, 10, 0, (i - 1) * 35)
    btn.BackgroundColor3 = Color3.fromRGB(70, 70, 100)
    btn.TextColor3 = Color3.new(1, 1, 1)
    btn.Font = Enum.Font.Gotham
    btn.TextSize = 16

    btn.MouseButton1Click:Connect(function()
        if info.url then
            pcall(function()
                loadstring(game:HttpGet(info.url))()
            end)
        elseif info.coordinates then
            local char = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
            if char and char:FindFirstChild("HumanoidRootPart") then
                char:MoveTo(info.coordinates)
            end
        end
    end)
end

-- Exploits Section
local espToggle = false
local killAuraToggle = false

-- ESP Toggle Button
local espButton = Instance.new("TextButton", TabFrames["Exploits"])
espButton.Text = "Toggle ESP"
espButton.Size = UDim2.new(1, -20, 0, 30)
espButton.Position = UDim2.new(0, 10, 0, 10)
espButton.BackgroundColor3 = Color3.fromRGB(80, 80, 120)
espButton.TextColor3 = Color3.new(1, 1, 1)
espButton.Font = Enum.Font.GothamBold
espButton.TextSize = 16
espButton.MouseButton1Click:Connect(function()
    espToggle = not espToggle
    if espToggle then
        espButton.Text = "ESP: On"
        -- Add ESP code here (e.g., show item/player/monster ESP)
    else
        espButton.Text = "ESP: Off"
        -- Disable ESP
    end
end)

-- KillAura Toggle Button
local killAuraButton = Instance.new("TextButton", TabFrames["Exploits"])
killAuraButton.Text = "Toggle KillAura"
killAuraButton.Size = UDim2.new(1, -20, 0, 30)
killAuraButton.Position = UDim2.new(0, 10, 0, 50)
killAuraButton.BackgroundColor3 = Color3.fromRGB(80, 80, 120)
killAuraButton.TextColor3 = Color3.new(1, 1, 1)
killAuraButton.Font = Enum.Font.GothamBold
killAuraButton.TextSize = 16
killAuraButton.MouseButton1Click:Connect(function()
    killAuraToggle = not killAuraToggle
    if killAuraToggle then
        killAuraButton.Text = "KillAura: On"
        -- Add KillAura code here (e.g., deal damage to nearby mobs)
    else
        killAuraButton.Text = "KillAura: Off"
        -- Disable KillAura
    end
end)

-- Get Horse Class Button
local horseClassButton = Instance.new("TextButton", TabFrames["Exploits"])
horseClassButton.Text = "Get Horse Class (Lobby)"
horseClassButton.Size = UDim2.new(1, -20, 0, 30)
horseClassButton.Position = UDim2.new(0, 10, 0, 90)
horseClassButton.BackgroundColor3 = Color3.fromRGB(80, 80, 120)
horseClassButton.TextColor3 = Color3.new(1, 1, 1)
horseClassButton.Font = Enum.Font.GothamBold
horseClassButton.TextSize = 16
horseClassButton.MouseButton1Click:Connect(function()
    -- Run code to buy Horse class in the lobby
    local args = {
        [1] = "Horse"
    }

    game:GetService("ReplicatedStorage"):WaitForChild("Shared"):WaitForChild("RemotePromise"):WaitForChild("Remotes"):WaitForChild("C_BuyClass"):FireServer(unpack(args))
end)

-- Fullbright Toggle
local fullbright = false
local brightnessButton = Instance.new("TextButton", TabFrames["Exploits"])
brightnessButton.Text = "Toggle Fullbright"
brightnessButton.Size = UDim2.new(1, -20, 0, 30)
brightnessButton.Position = UDim2.new(0, 10, 0, 130)
brightnessButton.BackgroundColor3 = Color3.fromRGB(80, 80, 120)
brightnessButton.TextColor3 = Color3.new(1, 1, 1)
brightnessButton.Font = Enum.Font.GothamBold
brightnessButton.TextSize = 16
brightnessButton.MouseButton1Click:Connect(function()
    fullbright = not fullbright
    if fullbright then
        Lighting.Brightness = 2
        Lighting.ClockTime = 14
        Lighting.FogEnd = 100000
    else
        Lighting.Brightness = 1
        Lighting.ClockTime = 10
        Lighting.FogEnd = 500
    end
end)

-- Credits
local creditsTitle = Instance.new("TextLabel", TabFrames["Credits"])
creditsTitle.Text = "Credits"
creditsTitle.Size = UDim2.new(1, -20, 0, 30)
creditsTitle.Position = UDim2.new(0, 10, 0, 10)
creditsTitle.BackgroundTransparency = 1
creditsTitle.TextColor3 = Color3.new(1, 1, 1)
creditsTitle.Font = Enum.Font.GothamBold
creditsTitle.TextSize = 22
creditsTitle.TextWrapped = true

local creditsLabel = Instance.new("TextLabel", TabFrames["Credits"])
creditsLabel.Text = "GUI Designer: DeathlordYT\nScripter: Ringta and Deathlord\nDiscord: discord.gg/ringta"
creditsLabel.Size = UDim2.new(1, -20, 1, -50)
creditsLabel.Position = UDim2.new(0, 10, 0, 50)
creditsLabel.BackgroundTransparency = 1
creditsLabel.TextColor3 = Color3.new(1, 1, 1)
creditsLabel.Font = Enum.Font.Gotham
creditsLabel.TextSize = 16
creditsLabel.TextWrapped = true
creditsLabel.TextYAlignment = Enum.TextYAlignment.Top

-- Visibility Buttons
MinimizeButton.MouseButton1Click:Connect(function()
    MainFrame.Visible = false
    HubButton.Visible = true
end)
CloseButton.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)
HubButton.MouseButton1Click:Connect(function()
    MainFrame.Visible = true
    HubButton.Visible = false
end)
